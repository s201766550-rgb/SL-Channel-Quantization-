# @package _global_
profile:
  name: smoke

dataset:
  name: mnist

training:
  rounds: 2
  batch_size: 32
  eval_interval: 1

client:
  num_clients: 2

server:

compression:
  num_groups: 2

acii:
  history_window: 2

